<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="./css/solicitar.css">
  <title>Empleado</title>
</head>
<body>
  <%- include('partials/sidebar') %>

  <div class="content principal">
    <%- include('partials/navbar') %>
    <div class="container">
      <div class="contenedor">
          <div class="titulo">
            <h3>Solicitar cambios en información bancaria</h3>
          </div>
      <div class="formularios">
        <div id="form1" class="form-section">
          <h3 class="nombre">Información de la nueva cuenta bancaria</h3>
          <div class="datosCuenta">
            <select id="accountType" required>
              <option value="">Seleccionar Banco</option>
              <option value="current">BBVA</option>
              <option value="savings">Bancolombia</option>
            </select>
            <select id="accountNumber" required>
              <option value="">Número de cuenta</option>
              <option value="current">156323536364</option>
          </select>
            <select id="accountType" required>
                <option value="">Tipo de cuenta</option>
                <option value="current">Cuenta corriente</option>
                <option value="savings">Cuenta de ahorros</option>
            </select>
          </div>
          <div class="posicion">
          <button class="cancelar" onclick="">Cancelar</button>
          <button onclick="nextForm(1)">Continuar</button>
        </div>
        </div>
      
        <div id="form2" class="form-section hidden">
          <div class="datoscuenta">
          <h3 class="nombre">Datos Cuenta</h3>
          <div class="archivos">
            <div class="contenedoropc">
              <h5>Certificación bancaria</h5>
              <p>Certificación bancaria actualizada a nombre de la razon social nueva (No mayor a 30 días)</p>
              <input type="file" id="bankCertificate" required>
              <p>PDF, JPG O PNG no mayor a 30MB</p>
            </div>
            <div class="contenedoropc">
              <h5>Copia de documento de identidad</h5>
              <p>Documento de identificación actualizada</p>
              <input type="file" id="idDocument" required>
              <p>PDF, JPG O PNG no mayor a 30MB</p>
            </div>
          </div>
          </div>
          <div class="posicion1">
            <button class="cancelar" onclick="">Cancelar</button>
            <button onclick="nextForm(2)">Continuar</button>
          </div>
        </div>
      
        <div id="form3" class="form-section hidden">
          <h3>Confirma de solicitud</h3>
          <p>El proceso de cambio podría tardar hasta 72 horas, puedes ver el estado en la pestaña de Solicitud de cambios.</p>
          <div class="contenedorC">
            <div class="info">
              <span>Información Bancaria</span>
              <p><span>Banco:</span> BBVA</p>
              <p><span>Cuenta:</span> 156323536364</p>
            </div>
            <div class="documentos">
              <span>Domumentos</span>
              <p class="nombreArchivo">NombreDeArchivo.pdf</p>
              <p class="nombreArchivo">NombreDeArchivo.pdf</p>
            </div>
          </div>
          <div class="aceptar">
            <input type="radio">
            <p>Autorizo a Apex para que realice la modificación de la cuenta bancaria.</p>
          </div>
          <div class="posicion1">
            <button class="cancelar" onclick="">Cancelar</button>
            <button onclick="submitForms()">Enviar</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function nextForm(currentForm) {
    const currentSection = document.getElementById(`form${currentForm}`);
    let isValid = true;
    currentSection.querySelectorAll('input[required], select[required]').forEach(input => {
        if (!input.value) {
            isValid = false;
            input.classList.add('error');
        }
    });

    if (isValid) {
        currentSection.classList.remove('active');
        const nextSection = document.getElementById(`form${currentForm + 1}`);
        if (nextSection) {
            nextSection.classList.add('active');
        }
    }
}
function submitForms() {
    alert('Formularios enviados correctamente');
}
document.getElementById('form1').classList.add('active');

</script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
