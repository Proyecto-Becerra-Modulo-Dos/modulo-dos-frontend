<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/pago.css">
  <title>Solicitud de permiso de ausencia</title>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    .header {
      background-color: white;
      padding: 10px;
      border: black solid 1px;
      border-radius: 14px;
      display: flex;

      justify-content: center;
      margin-bottom: 20px;
      margin-top: 10px;
    }

    .back-link {
      padding-top: 0;
      text-decoration: none;
      font-size: xx-large;
      font-weight: bold;
      color: #007bff;
      margin-right: 10px;
    }

    h1 {
      font-size: 18px;
      margin: 0;
    }

    form {
      background-color: white;
      padding: 10px;
      display: flex;
      flex-direction: column;
      border: black solid 1px;
      border-radius: 16px;
    }

    label {
      margin-top: 10px;
      font-size: 14px;
    }

    input,
    select {
      margin-top: 5px;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    button {
      margin-top: 20px;
      padding: 10px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    .alert {
      padding: 15px;
      margin-bottom: 20px;
      border: 1px solid transparent;
      border-radius: 4px;
    }

    .alert-success {
      color: #155724;
      background-color: #d4edda;
      border-color: #c3e6cb;
    }

    .alert-error {
      color: #721c24;
      background-color: #f8d7da;
      border-color: #f5c6cb;
    }
  </style>
</head>

<body>
  <%- include('partials/sidebar') %>
    <div class="content principal">
      <%- include('partials/navbar') %>

        <div class="container">
          <div class="contenedor">
            <a href="#" class="back-link">← Volver</a>
            <div class="header">
              <h1>Solicitud de Permiso de Ausencia</h1>
            </div>
            <form id="permisoForm" class="formulario" onsubmit="permiso_ausencia(event)">
              <label for="fecha">Fecha:</label>
              <input type="date" id="fecha" name="fecha" required>

              <label for="tipo-permiso">Tipo de permiso:</label>
              <select id="tipo-permiso" name="tipo-permiso" required>
                <option value="">Seleccione el tipo</option>
                <option value="cita medica">Cita Médica</option>
                <option value="vacaciones">Vacaciones</option>
                <option value="enfermedad">Enfermedad</option>
                <option value="personal">Personal</option>
              </select>

              <label for="motivo">Motivo de la Ausencia:</label>
              <textarea id="motivo" name="motivo" rows="5" cols="50" required></textarea>

              <button type="submit">Enviar solicitud</button>
              <input type="hidden" id="url" value="<%= url %>">
            </form>
          </div>
        </div>
    </div>
    <div id="mensaje-alert" style="display: none;" class="alert"></div>

    <script src="/js/permisoAusencia"></script>

    <script src=" https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
